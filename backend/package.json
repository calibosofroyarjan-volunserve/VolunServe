{
  "name": "volunserve-identity-backend",
  "version": "1.0.0",
  "private": true,
  "description": "VolunServe secure identity verification backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check firebaseAdmin.js && node --check didit.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "firebase-admin": "^14.3.0"
  }
}